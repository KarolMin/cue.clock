// Caps the main content column width so phone-designed layouts don't stretch
// awkwardly full-bleed across a tablet screen; content stays centered instead.
export const MAX_CONTENT_WIDTH = 480;

// Wider cap used by the match screen's landscape layout (tablets, or a phone
// rotated sideways), which spreads player panels either side of the clock
// instead of stacking everything in a single narrow column.
export const MAX_CONTENT_WIDTH_LANDSCAPE = 920;
